{
  "name": "koelnr-app",
  "private": true,
  "scripts": {
    "dev": "bun run --filter './apps/*' dev",
    "build": "bun run --filter './apps/*' build",
    "lint": "bun run --filter './apps/*' lint"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
